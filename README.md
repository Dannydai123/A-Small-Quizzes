## next.js

tried to use flowbite-react as a react component libary.

not very good. didnt know what tailwind CSS attributes being used. needs to dive into the source code to analyse createJSX statement. not very user-friendly

eliminated flowbite-react soon

NOTE that FLOWBITE-REACT is react component lib, while FLOWBITE is a pure CSS/vanilla JS lib, just like bootstrap5,  but flowbite is based on tailwind


